# Bank-Account-API

SpringBoot Bank app developed using hexagonal architecture.

To launch the project (Potsgres-latest and Spring app), run 'docker compose up --build -d' : two containers 
should be created, then you can call the API through the postman export.

Done in : Java21, Spring 3.5.9, Liquibase 4.33.
